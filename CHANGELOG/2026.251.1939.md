# 2026.251.1939 — ci: install cue (pinned) before the params reproducibility gate

## Summary

The cue:gen reproducibility step in ci.yml ran `cue exp gengotypes` without cue on the runner. Two root causes were fixed across heads: (1) no cue binary → `go install cuelang.org/go/cmd/cue@v0.16.1` (the charly-pinned version), without a custom GOBIN (the first attempt's GOBIN=/usr/local/go/bin is not on the runner PATH); (2) the earlier patch left four dead `/tmp/cuegen` lines after the new diff gate — removed in this head. The root `charly.yml` schema stamp is bumped to 2026.249.2125 (the latest charly release's floor, required by consumers validating with a fresh binary).

Files: `.github/workflows/ci.yml` (repro gate now a single clean step) and `charly.yml` (version stamp).

## How tested

Live CI on this PR executes the fixed steps (`test` job: gofmt/vet/test → Install cue → repro). Local repro of the diff gate with real command output (an identical diff prints nothing; exit code is the evidence):

```
$ cd /tmp/cuegenpkg && /home/atrawog/go/bin/cue exp gengotypes .
$ diff <(sed '1,1d' /tmp/cuegenpkg/cue_types_params_gen.go) <(sed '1,1d' /tmp/plugin-review/candy/plugin-review/params/cue_types_gen.go); echo "diff_exit=$?"
diff_exit=0
$ cd /tmp/plugin-review/candy/plugin-review && go test ./...
ok  github.com/opencharly/plugin-review/candy/plugin-review
```

## Rulebook compliance

- R1: both CI failures were RCA'd (missing cue; misplaced GOBIN; dead leftover lines from a partial patch) — never code defects; the test job stayed green throughout.
- R3: the single repro gate now owns the check; no duplication.
- PR-only landing via the org validator.

## Change classification

- Change class: CI/config only (no runtime code change).
- Verification gate: the live `test` check run on this PR + the pasted local repro (real output, exit code).
- Attribution tier: `analysed on a live system` — the changed CI path executes live on this PR.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*