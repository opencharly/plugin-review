# 2026.266.2359 — deps(review): pin plugin-gh v0.2026266.2326 to inherit the shared response cache

## Summary

Bump `github.com/opencharly/plugin-gh` in `candy/plugin-review/go.mod` from `v0.2026263.1602` to `v0.2026266.2326`, so plugin-review inherits plugin-gh's shared `spec/cache` ArtifactStore response cache (the cache consolidation shipped in plugin-gh#5 / tag `v0.2026266.2326`).

This is a pure Go-module dependency bump. The diff makes **five** changes, all mechanical consequences of the one direct bump — the body names and justifies each:

1. `github.com/opencharly/plugin-gh` `v0.2026263.1602` → `v0.2026266.2326` — **direct** require; the intended bump.
2. `github.com/opencharly/sdk` `v0.2026264.731` → `v0.2026266.1111` — **transitive** upgrade: plugin-gh requires this sdk, and under MVS `go get` raises the whole module graph to satisfy it.
3. `github.com/opencharly/spec` `v0.2026262.957` → `v0.2026266.1306` — **transitive** upgrade for the same reason (plugin-gh requires this spec).
4. `github.com/opencontainers/go-digest v1.0.0 // indirect` — **new indirect** require, pulled in by plugin-gh's ArtifactStore/OCI-layout cache dependency.
5. `github.com/opencontainers/image-spec v1.1.1 // indirect` — **new indirect** require, same origin.

(`go.sum` changes are the matching checksums for the five go.mod lines; no other file changes.)

## How tested

**Exact reproduction of the diff from `origin/main`** — the commands that actually produced the three upgraded requires (Go 1.16+ does not mutate `go.mod` on `build`/`test`, so the earlier body's command list was wrong; this is the real sequence, with its real output):

```
$ git archive origin/main candy/plugin-review | tar -x -C /tmp/repro
$ cd /tmp/repro/candy/plugin-review && export GOFLAGS=-mod=mod
$ go get github.com/opencharly/plugin-gh@v0.2026266.2326
go: upgraded github.com/opencharly/plugin-gh v0.2026263.1602 => v0.2026266.2326
go: upgraded github.com/opencharly/sdk v0.2026264.731 => v0.2026266.1111
go: upgraded github.com/opencharly/spec v0.2026262.957 => v0.2026266.1306
$ go mod tidy
$ diff go.mod <(git -C <worktree> show HEAD:candy/plugin-review/go.mod)
IDENTICAL go.mod
```

**Build / vet / test, real output:**

```
$ go build ./...      # exit 0
$ go vet ./...        # exit 0
$ go test ./...
ok  	github.com/opencharly/plugin-review/candy/plugin-review	0.033s
?   	github.com/opencharly/plugin-review/candy/plugin-review/cmd/serve	[no test files]
```

**Changed runtime path exercised LIVE** (the plugin-gh client this bump pulls in), against real GitHub with a real token — not a fake, and not silently skipped:

```
$ export GH_TOKEN="$(gh auth token)"
$ go test -run TestFilesRecoversOmittedPatchLive -v ./...
=== RUN   TestFilesRecoversOmittedPatchLive
    ghclient_live_test.go:39: recovered charly.yml patch: 113907 bytes, NoPatch=false
--- PASS: TestFilesRecoversOmittedPatchLive (1.43s)
```

## Rulebook compliance

- **R0** — loaded `/charly-internals:git-workflow`, `/charly-internals:go`.
- **R1** — the prior BLOCK was accurate: the body under-counted the diff. RCA'd to MVS transitive upgrades + ArtifactStore indirect deps; the reproduction above shows exactly how the five lines arise.
- **R2** — the whole bump is here.
- **R3** — plugin-gh is the single implementation; plugin-review consumes it by pin.
- **R4** — no workaround; reproduction is deterministic.
- **R5** — the stale pin is the only change.
- **R6** — `fix/pluginreview-ghpin` off `origin/main`; no force-push.
- **R7 / R7a** — `go build` / `go vet` / `go test` green on the final tree.
- **R8 / R9** — dependency-pin change; no label/runtime dependency change.
- **Live or skip** — the live GitHub read ran against the REAL service (token present) and PASSED; not mocked, not skipped.

## Change classification

- **Change class:** Go module dependency bump (`go.mod` / `go.sum` only).
- **Verification gate:** exact `go get` + `go mod tidy` reproduction + `go build`/`go vet`/`go test` + the live `TestFilesRecoversOmittedPatchLive` against real GitHub.
- **Attribution tier:** `analysed on a live system` — the changed runtime path (the plugin-gh client) ran live with retained output; a full charly R10 bed for plugin-review was not run.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*